// Student C — Displays AI score (color-coded), pros, cons, and summary
// TODO: Implement

export default function ScoreDisplay({ score, pros, cons, summary }) {
  return <div>Score: {score ?? 'Not yet scored'}</div>;
}
