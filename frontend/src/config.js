// Placeholder config values
// These will be replaced with real values later

const config = {
  API_BASE_URL: "https://example.com/api",
  COGNITO_USER_POOL_ID: "placeholder-user-pool-id",
  COGNITO_APP_CLIENT_ID: "placeholder-app-client-id",
  S3_PHOTOS_BUCKET: "placeholder-bucket-name",
};

export default config;

Add placeholder frontend config
